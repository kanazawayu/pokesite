.search
  = form_with(url: articles_searches_path, local: true, method: :get, class: "search__form") do |form|
    = form.text_field :keyword, placeholder: "投稿を検索する", class: "search__form__input"
    = form.submit "検索", class: "search__form__btn"
.content
  %h4
    検索結果一覧
  .content__post
    - @articles.each do |article|
      = render partial: "articles/article", locals: { article: article }